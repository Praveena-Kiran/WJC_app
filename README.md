# Woxsen Japanese Club (WJC) App

## Structure

```
├── archive/     - Old JS prototype (legacy)
├── mobile/      - Expo Go (React Native) app
├── web/         - Next.js web app (deprecated — no web version planned)
```
